{
  "name": "staticman-frama-dev",
  "description": "Auto build PR",
  "scripts": {
  },
  "env": {
    "GITHUB_TOKEN": {
      "required": true
    },
    "GITLAB_TOKEN": {
      "required": true
    },
    "NODE_ENV": {
      "required": true
    },
    "RSA_PRIVATE_KEY": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "stack": "heroku-18",
  "buildpacks": [

  ]
}
